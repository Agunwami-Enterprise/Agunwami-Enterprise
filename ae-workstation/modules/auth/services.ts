// TODO (Stage 3): Implement Firebase Auth sign-in, sign-out, and session management.
// All sensitive operations (role assignment, account creation) must go through
// Firebase Cloud Functions — never direct client-side Firestore writes.

export {};
