// ─── Shift Period Constants ───────────────────────────────────────────────────
// These are the fallback values used when a staff member has not been assigned
// a custom shift period by an admin.

export const DEFAULT_SHIFT_START = '09:00' as const;
export const DEFAULT_SHIFT_END   = '17:00' as const;
