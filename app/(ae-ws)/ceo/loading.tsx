import Preloader from '@/app/components/ceo/Preloader';

// Next.js route-level loading state — shown while any CEO page segment is loading.
export default function Loading() {
  return <Preloader />;
}
