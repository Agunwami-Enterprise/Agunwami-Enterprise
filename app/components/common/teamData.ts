/**
 * Re-exports from the canonical data source at lib/data/teamData.ts.
 * Import from there directly for new code.
 */

export type { TeamMember } from "@/lib/data/teamData";
export { defaultTeam } from "@/lib/data/teamData";